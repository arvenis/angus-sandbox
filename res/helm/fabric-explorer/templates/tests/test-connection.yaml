apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fabric-explorer.fullname" . }}-test-connection"
  labels:
{{ include "fabric-explorer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fabric-explorer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
