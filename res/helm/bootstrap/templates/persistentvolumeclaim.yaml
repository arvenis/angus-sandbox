kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: fabric-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: hostpath
  selector:
    matchLabels:
      name: fabric
  resources:
    requests:
      storage: 1Gi